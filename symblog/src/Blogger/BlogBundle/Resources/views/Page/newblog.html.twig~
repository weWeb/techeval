{# src/Blogger/BlogBundle/Resources/views/Page/newblog.html.twig #}
{# Reference: http://tutorial.symblog.co.uk/docs/configuration-and-templating.html #}
 
{% extends 'BloggerBlogBundle::layout.html.twig' %}

{% block title %}Add New Blog{% endblock%}

{% block body %}
    <header>
        <h2>Add a new Entry</h2>
    </header>


{% autoescape true %}
    <form action="{{ path('BloggerBlogBundle_newblog') }}" method="post" {{ form_enctype(form) }} class="newblog">
        {{ form_errors(form) }}

	{# form_row(form.author) #}
	<div>
		{{ form_label(form.author) }}
		{{ form_errors(form.author) }}
		{{ form_widget(form.author, {'attr': {'value': '%currentuser%' }})|replace({'%currentuser%': app.user.username})|raw }}
	</div>
        {{ form_row(form.title)}}
        {{ form_row(form.blog) |raw }}

        {{ form_rest(form) }}
	
	
	
        <div><input type="submit" value="Submit" /></div>
    </form>
    {{ tinymce_init() }}
    {% endautoescape%}
{% endblock %}

{% block sidebar %}
    {# empty content #}
{% endblock %}
